# Notas

Este es mi primer programa en NodeJS

```
Options:
      --help     Show help                                     [boolean]
      --version  Show version number                           [boolean]
  -b, --base     Es la base para la tabla de multiplicar
                                                     [number] [required]
  -l, --listado  Muestra la lista             [boolean] [default: false]
  -h, --hasta    Hasta donde llegan las tablas    [number] [default: 10]
```